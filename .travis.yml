language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-webappinator' ]; then cd .. && eval "mv $currentfolder generator-webappinator" && cd generator-webappinator; fi

